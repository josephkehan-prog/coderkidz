name = "coderkidz-platform"
main = "src/index.ts"
compatibility_date = "2024-11-01"

# Local dev works out of the box (`pnpm platform:dev` uses a local SQLite);
# the id below points at the live D1 instance used by `wrangler deploy`.
[[d1_databases]]
binding = "DB"
database_name = "coderkidz-platform"
database_id = "7b795463-7b33-4071-a64d-5bbdbfeadfd4"
